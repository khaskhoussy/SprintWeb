{% extends 'front.html.twig' %}

{% block content %}
    <h1>Gestedumois</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gesteDuMois.id }}</td>
            </tr>
            <tr>
                <th>Nomgeste</th>
                <td>{{ gesteDuMois.nomGeste }}</td>
            </tr>
            <tr>
                <th>Moisgeste</th>
                <td>{{ gesteDuMois.moisGeste }}</td>
            </tr>
            <tr>
                <th>Descgeste</th>
                <td>{{ gesteDuMois.descGeste }}</td>
            </tr>
            <tr>
                <th>Imagegeste</th>
                <td>{{ gesteDuMois.imageGeste }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('front_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('front_edit', { 'id': gesteDuMois.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
