{% extends 'back.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-lg-6">
            <h1>Packdecoration {{ packDecoration.nomPack }}</h1>

            {{ form_start(edit_form) }}
            <div class="form-group">
                <label for="exampleInputEmail1">Nom</label>
                {{ form_widget(edit_form.nomPack, {'attr': {'class': 'form-control','aria-describedby':'nom','placeholder':'Enter nom'}}) }}
                <small id="emailHelp" class="form-text text-muted">We'\ll never share your email with anyone else.</small>
            </div>
            <div class="form-group">
                <label for="exampleInputPassword1">Type</label>
                {{ form_widget(edit_form.typep, {'attr': {'class': 'form-control','aria-describedby':'type','placeholder':'choisir type'}}) }}
            </div>
            <div class="form-group">
                <label for="exampleInputPassword1">Description</label>
                {{ form_widget(edit_form.descriptionPack, {'attr': {'class': 'form-control','aria-describedby':'description','placeholder':'Entrer description'}}) }}
            </div>
            <div class="form-group">
                <label for="exampleInputPassword1">Prix</label>
                {{ form_widget(edit_form.prixp, {'attr': {'class': 'form-control','aria-describedby':'prix','placeholder':'Entrer prix'}}) }}
            </div>
            <div class="form-group">
                <label for="exampleInputPassword1">Image</label>
                {{ form_widget(edit_form.imagePack, {'attr': {'class': 'form-control','aria-describedby':'image','placeholder':'Choisir image'}}) }}
            </div>
            <button class="btn btn-sm btn-primary" type="submit" value="Create" class="btn btn-primary">Submit</button>
            {{ form_end(edit_form) }}



                    <a class="btn btn-sx btn-success" href="{{ path('back_paysagiste_index') }}">Back to the list</a>


                    {{ form_start(delete_form) }}
                    <input class="btn btn-sx btn-danger" type="submit" value="Delete">
                    {{ form_end(delete_form) }}


        </div>
    </div>
{% endblock %}
